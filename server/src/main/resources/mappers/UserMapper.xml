<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.thanos.springboot.dao.UserMapper">
  <resultMap id="UserResultMap" type="User">
    <id column="id" property="id" />
    <result column="name" property="name" />
    <result column="sex" property="sex" />
    <result column="descp" property="descp" />
  </resultMap>

  <sql id="Base_Column_List" >
    id, name, sex, descp
  </sql>

  <select id="findById" resultMap="UserResultMap">
    select id, name, sex, descp
    from user
    where id = #{id}
  </select>

  <select id="findAll" resultMap="UserResultMap">
    select id, name, sex, descp
    from user
  </select>

  <insert id="insert" parameterType="com.thanos.springboot.bo.Operation" >
    insert into user (name, sex, descp)
    values (#{name}, #{sex}, #{descp})
  </insert>

</mapper>
